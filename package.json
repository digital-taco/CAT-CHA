{
  "name": "cat-cha",
  "version": "0.1.0",
  "description": "Basically reCAPTCHA but with cats, because this is the internet and we can do whatever we want.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently 'PORT=5001 nodemon server.js' '(cd client && npm start)'",
    "build": "cd client && npm run build",
    "postinstall": "cd client && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/digital-taco/CAT-CHA.git"
  },
  "keywords": [
    "reCAPTCHA",
    "cats"
  ],
  "author": "digital-taco",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/digital-taco/CAT-CHA/issues"
  },
  "homepage": "https://github.com/digital-taco/CAT-CHA#readme",
  "dependencies": {
    "axios": "^0.22.0",
    "concurrently": "^6.3.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.13"
  }
}
